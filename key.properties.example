# Copy to key.properties in this directory (project root). Do not commit key.properties.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=upload
storeFile=../../upload-keystore.jks
